#!/usr/bin/env python3
"""B v2·否决层判据细化（前后对比用，不入生产）。

张耀明 0630：B v1 守住 G6 生死线、剥出 G7，但 #58(G5→G6) 稳稳漏切——根因是否决层把
「产物缺陷→返工」和「真·返工链」当成一回事，一律 keep。本版**只改否决层一个语义判据**
（不加词法/魔数）：把「是不是返工」细化成
  A 为同一个验收目标返工（手段变、验收点不变）→ keep；
  B 刀前验收目标已收口、刀后冲一个新验收标准 → cut。
其余（提议层、投票、默认 keep 兜底、数据加载）全部复用 v1，逐字不动。

复跑两承重 case：
  · #44-89：#58 应切出（G5 收口→G6 新标准保真），#82 仍切（G6→G7）→ 期望 3 段；
  · G6[58-81]：内部 12 刀仍须全否（手段在变但验收点始终=回退因果不被切碎）→ 生死线，1 段。
"""
import boundary_review_B_proto as B

# 仅替换否决层 prompt；_veto_vote 在调用时读模块全局，赋值即生效。
B._VETO_SYS = """你是对话轨迹切分的【边界否决员】，纪律是保返工链、宁可不切。给你「刀前一小段」和「刀后一小段」，\
判断刀后该不该独立成段。

**核心区分（判断的关键）——看刀后在为「什么」努力：**
A.（→否决 keep）还在为刀前**同一个验收目标**努力：换手段、补细节、加代码兜底、再验证、被提醒后回退重做——\
但「要达成/要满足的那个验收点」没变。这是返工链，必须保住。
   例：目标=「回退因果不被切碎」，其下调prompt→换模型→加代码兜底→补判据→留探针，手段一直换，\
   但验收点自始至终是同一个 → 全程一个目标，一刀不切。
B.（→放行 cut）刀前那个验收目标**已经达成/收口**，刀后引入的是一个**新的、之前不存在的验收标准**\
（要解决的问题变了、要满足的标准变了）——**哪怕还在同一个大主题、同一个产物上**。
   例：刀前已把第一版切分表跑出来（「能调LLM切出task11表」已达成），刀后转去攻「这版表得保住回退因果保真」\
   ——这是一个之前不存在的新验收标准，不是在修第一版表本身的 bug。

**判断步骤：**
1) 先问：刀前那个要达成的目标，到刀前段末尾**算不算已经做出来/收口了**？
2) 没收口、刀后还在把它往做成里推 → A，否决；已收口、刀后冲的是**另一个新标准** → B，放行。

**底线（不变）：** 拿不准刀后到底是「返工旧目标」还是「攻新标准」时 → **默认否决 keep**，把返工链切碎比漏切一刀严重。\
但反过来——「要满足的验收标准实打实换了」就是新目标，**不要只因为还在同一个大主题/同一个产物上工作，就把它误当返工**。

输出严格 JSON（不要 markdown、不要解释）：\
{"new_goal": true/false, "reason": "<一句话：刀前目标收口没/刀后攻旧目标还是新标准>"}"""

if __name__ == "__main__":
    print("[B v2·否决判据细化]")
    B.main()
